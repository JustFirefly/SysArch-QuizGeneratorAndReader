import os

#Step 0. User sets quiz parameters.
    #Step 0.1 Select number of questions.
    #Step 0.2 Select types of questions.

number_questions = input("Number of Questions (default is 10) >> ")
types_of_questions = input("Select types of questions using quiz weeks e.g. 12 would make a quiz of questions from quizzes 1 and 2 >> ")
#Step 1. get database of questions.
#Step 2. create quiz based on User parameters.